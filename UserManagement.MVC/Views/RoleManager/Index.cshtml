@model IEnumerable<UserManagement.MVC.Models.Bus>
@{
    ViewData["Title"] = "Add Buses";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Add Bus</h1>
<form method="post" asp-action="AddBus" asp-controller="RestaurantManager">
    <div class="input-group">
        <input name="restaurant_name" class="form-control w-25">
        <span class="input-group-btn">
            <button class="btn btn-info">Add new restaurant</button>
        </span>
    </div>
</form>

<table class="table table-striped">
    <thead>
        <tr>

            <th>Id</th>
            <th>Buses</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var bus in Model)
        {
            <tr>
                <td>@bus.model</td>
            </tr>
        }

    </tbody>
</table>